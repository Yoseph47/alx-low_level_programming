#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/** 
*mainprint-> prints the last digit of the number stored in 
*the variable n
*Return -> always returns 0
*/
int main(void)
{
int n;  
srand(time(0));  
n = rand() - RAND_MAX / 2;  
 printf("last digit of %d" , n);
 if (n>5)
   {
     printf("greater than 5");
   }
 if (n == 0)
   {
     printf("0")}
 
 if (n<6 && n!=0)
   {
     print f("less ")
   }
       }
return (0);
}